<% @users.each do |user| %>
<ul>
  <li>Name:<%= link_to user.first_name, user_path(user) %> <%= user.last_name %> </li>
  <li> <%= link_to(image_tag(user.profile_picture), "/users/#{user.id}") %></li>
</ul>
<% end %>
