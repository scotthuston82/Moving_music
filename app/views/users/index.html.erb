<% @users.each do |user| %>
  <ul>
    <li>Name: <%= link_to user.stage_name, user_path(user) %></li>
    <li> <%= link_to(image_tag(user.profile_picture), "/users/#{user.id}") %></li>
  </ul>
<% end %>
